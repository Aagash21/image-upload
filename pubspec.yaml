name: new_image_upload_app
description: A new Flutter project to upload images

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.1
  firebase_core: ^3.6.0
  firebase_storage: ^12.3.3
  image_picker: ^1.1.2
  path: ^1.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0  # Downgraded lints

flutter:
  uses-material-design: true
